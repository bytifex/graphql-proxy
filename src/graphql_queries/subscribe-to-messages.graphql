subscription subscribeToMessages(
	$messageFilters: [MessageFilter!]!
) {
	messages(
		messageFilters: $messageFilters
	) {
		connectionId
		sequenceCounter
		connectionType
		messageDirection
		message
	}
}
